<div class="container-fluid">
	<section>
		<h1>Lista de usuarios</h1>
	</section>
  <div class="table-responsive-sm">
		<table class="table table-hover">
			<tr>
				<thead>
					<th>Name<th>
					<th>Surname<th>
					<th>N° Doc<th>
					<th>Phone<th>
					<th>Options<th>
				</thead>
			</tr>
			<tr>
				<% @users.each do |user| %>
					<td><%= user.name %><td>
					<td><%= user.surname %><td>
					<td><%= user.document %><td>
					<td><%= user.phone %><td>
					<td>
						<%= link_to 'Edit', edit_user_path(user) %>
						<%= link_to 'Delete', user, method: :delete, data:{confirm:"Confirm delete"} %>
					<td>
			</tr>
			<% end %>
		</table>
	</div>

</div>
